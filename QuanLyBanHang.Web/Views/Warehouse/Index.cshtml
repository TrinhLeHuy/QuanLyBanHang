@model IEnumerable<QuanLyBanHang.Data.Entities.Warehouse>

@{
    ViewData["Title"] = "Quản lý Kho hàng";
}

<h2 class="text-center">🏬 Danh Sách Kho Hàng</h2>

<a class="btn btn-success mb-3" asp-action="Create">➕ Thêm Kho Mới</a>

<table class="table table-bordered table-hover">
    <thead class="table-light">
        <tr>
            <th>Mã Kho</th>
            <th>Tên Kho</th>
            <th>Vị Trí</th>
            <th>Điện Thoại</th>
            <th>Thao Tác</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.WarehouseId</td>
                <td>@item.WarehouseName</td>
                <td>@item.Location</td>
                <td>@item.Phone</td>
                <td>
                    <a class="btn btn-primary btn-sm" asp-action="Edit" asp-route-id="@item.WarehouseId">Sửa</a>
                    <a class="btn btn-danger btn-sm" asp-action="Delete" asp-route-id="@item.WarehouseId"
                       onclick="return confirm('Bạn có chắc muốn xóa kho này không?')">Xóa</a>
                </td>
            </tr>
        }
    </tbody>
</table>
